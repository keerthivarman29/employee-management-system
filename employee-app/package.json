{
  "name": "employee-app",
  "version": "1.0.0",
  "description": "Fullstack Employee Management App (Express + React + SQLite)",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm install && npm start",
    "client": "cd client && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
